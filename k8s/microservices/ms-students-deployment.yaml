apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-students
  namespace: wep
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-students
  template:
    metadata:
      labels:
        app: ms-students
    spec:
      containers:
      - name: ms-students
        image: docker.io/library/wep-ms-students:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3001
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: wep-config
              key: NODE_ENV
        - name: PORT
          value: "3001"
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: wep-config
              key: DB_STUDENTS_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ms-students-service
  namespace: wep
spec:
  selector:
    app: ms-students
  ports:
  - protocol: TCP
    port: 3001
    targetPort: 3001
  type: ClusterIP
