/**
 * Formatea una hora en base al valor proporcionado.
 * @param {string} value - Valor de hora a formatear.
 * @returns {string} Hora formateada (placeholder actualmente devuelve cadena vacía).
 */
export const formatHour = (value: string): string => {
    return '';
}

/**
 * Esta función retorna la fecha en formato dd/mm/yyyy
 * @example ```ts
 * formatDateDDMMYYYY('2026-06-05');
 * console.log('05/06/2026');
 * ```
 * @param {string} value - Fecha en formato ISO u otra representación legible.
 * @returns {string} Fecha formateada como dd/mm/yyyy.
 */
export const formatDateDDMMYYYY = (value: string): string => {
    return '';
}