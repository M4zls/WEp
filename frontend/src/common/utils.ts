export const formatHour = (value: string): string => {
    return '';
}

/**
 * Esta fucion retorna en formato dd/mm/yyyy
 * @example ```ts
 * formatDateDDMMYYYY();
 * console.log('Hola mundo');
 * ```
 * @param value 
 * @returns 
 */
export const formatDateDDMMYYYY = (value: string): string => {
    return '';
}